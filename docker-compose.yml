version: "3.9"

services:
  backend:
    build: ./server
    container_name: storefront-backend
    ports:
      - "5000:5000"
    volumes:
      - ./server:/app
      - /app/node_modules
    env_file:
      - ./server/.env
    depends_on:
      - mongo

  mongo:
    image: mongo:6
    container_name: storefront-mongo
    restart: always
    ports: 
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: